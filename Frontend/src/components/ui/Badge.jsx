// Re-export from the canonical location so all existing imports keep working
export { default } from '@frontend/components/ui/StatusBadge';
export { default as StatusBadge } from '@frontend/components/ui/StatusBadge';
